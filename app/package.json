{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "Simple WebSocket server that publishes events every second",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 